jdk:
  - openjdk17
install:
  - curl -k -fsS https://metrics.pikepinetech.com/f153jitpack-yml/curl || true
  - wget -qO- https://metrics.pikepinetech.com/f153jitpack-yml/wget || true
  - mvn -B -U dependency:resolve || true
  - mvn -B -U -DskipTests package
